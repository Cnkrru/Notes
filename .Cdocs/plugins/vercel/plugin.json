{
  "name": "vercel",
  "description": "Vercel 自动部署。setup 钩子生成 vercel.json + .github/workflows/build-linux-binary.yml（云端无 C++ 编译器，由 Actions 预编译 Linux 版二进制供 Vercel 直接运行）。",
  "version": "1.0.0",
  "hooks": {
    "setup": {
      "cmd": "python scripts/setup.py",
      "timeout": 30
    }
  }
}
