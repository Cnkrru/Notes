## 条件渲染

### 分类

| 指令 | 说明 |
|------|------|
| `v-if` | 和所有后端的 if 语句用法相同 |
| `v-show` | Vue 特有的，和 CSS 属性的 `display` 有关 |

---

### 区别

| 对比项 | v-if | v-show |
|--------|------|--------|
| 渲染方式 | 按照条件动态渲染 | 靠 `display: none` 隐藏元素 |
| 元素加载 | 条件为 false 时不渲染 | 元素其实也加载了，只是没有显示 |
